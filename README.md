```
chipProps: {
  classes:{
    root:string //override FormControl className,
    inputLabel:string //override InputLabel className,
    inputContainer:{
      root:string // override Input container root className,
      focused: string,
      underlineStyle: string,
      disabled: string,
      labeled: string,
      error: string
    }
    input:{
      root:string
      input:string
    },
    helperText:string
  }
}
```